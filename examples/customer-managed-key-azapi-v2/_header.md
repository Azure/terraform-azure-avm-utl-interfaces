# customer managed key Variant 2 example

This example creates a Key Vault key and user-assigned identity in the same apply as a container registry that consumes them. The interface performs no data-source lookups: the key URI and identity client ID flow directly from the resources that create them.
